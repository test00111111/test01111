@model test.ViewModel.CommonViewModel

@{
    ViewBag.Title = "產品列表";
}

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.carousel.min.css" />
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.theme.default.min.css" />

<script src="https://code.jquery.com/jquery-3.5.1.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/owl.carousel.min.js"></script>
<style>
    .owl-nav{
        display:none;        
    }
</style>

@*<div id="themeSlider" class="carousel slide mb-4 container-fluid" data-ride="carousel">
        <div class="carousel-inner ">
            @{foreach (var item in Model.carousels.Take(1))
                {
                    <div class="item active">
                        <img style="width:100%" src="~/Content/Img/@item.carouselImg" alt="First slide">
                    </div>
                }
            }
            @{foreach (var item in Model.carousels.Skip(1))
                {
                    <div class="item ">
                        <img style="width:100%" src="~/Content/Img/@item.carouselImg" alt="First slide">
                    </div>
                }
            }
        </div>
        <a class="carousel-control-prev" href="#themeSlider" data-slide="prev"></a>
        <a class="carousel-control-next" href="#themeSlider" data-slide="next"></a>

    </div>*@
<div class="owl-carousel owl-theme owl-nav">
    @{foreach (var item in Model.carousels)
        {
            <div class="item"><img style="width:100%" src="~/Content/Img/@item.carouselImg"></div>
        }
    }
</div>



<div class="row">
    <div class="col-md-12">
        <div class="page-header">
            <h2>最新上架商品 </h2>
        </div>
    </div>
</div>
<div class="row product-container">

    @{
        foreach (var item in Model.spu)
        {
            <div class="box col-12 col-sm-12 col-md-6 col-lg-4 col-xl-3 " style="margin-bottom:8px">
                <div class="thumbnail" style="width: 100%; ">
                    <img class="img-responsive" title="Click to View Product detail" style="cursor:pointer; width:100%; height:100%;"
                         src="~/Content/Img/@item.spuImg1" />
                    <div class="caption " style="height:40px">
                        <h5>@item.spuName</h5>
                    </div>
                    <div class="caption">
                        <p>$@item.spuPrice</p>
                    </div>
                    <div class="caption d-flex justify-content-between">
                        <button class="btn shadow-none" style="outline:0;"><i class="far fa-heart"></i></button>
                        <button class="btn shadow-none" style="outline:0;"><i class="fas fa-cart-plus"></i></button>
                    </div>
                </div>
            </div>
        }
    }
</div>
<div class="d-flex justify-content-center ">
    <button class="btn shadow-none" style="outline:0;">瀏覽更多</button>
</div>

<script>
    $(".owl-carousel").owlCarousel({
        autoplay:true,
        loop: true, // 循環播放
        margin: 10, // 外距 10px
        nav: true, // 顯示點點     
        
        responsive: {
            0: {
                items: 1 // 螢幕大小為 0~600
            },
            600: {
                items: 1 // 螢幕大小為 600~1000
            },
            1000: {
                items: 1 // 螢幕大小為 1000 以上
            }
        }
    });
</script>